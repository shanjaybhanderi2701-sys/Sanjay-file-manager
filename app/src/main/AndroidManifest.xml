<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Least-privilege storage access (FR-1.1, architecture §5). Granular media
      reads on API 33+; legacy READ_EXTERNAL_STORAGE only through API 32. Anything
      outside shared media is reached via SAF, which needs no manifest permission.
      MANAGE_EXTERNAL_STORAGE is intentionally absent from this Play-default build
      and is contributed only by the gated `fullaccess` flavor.
    -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Partial ("selected photos") access on API 34+. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!--
      Background copy/move/delete run as a WorkManager foreground service so they
      survive backgrounding and process death (FR-3.5, NFR-2.3). The DATA_SYNC
      type is required from API 34; POST_NOTIFICATIONS lets the progress
      notification show on API 33+. WorkManager itself contributes the
      SystemForegroundService declaration via manifest merge.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".FiloraApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Filora">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Filora">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Internal share/open via content URIs. Never exported (A1/§6.5). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
