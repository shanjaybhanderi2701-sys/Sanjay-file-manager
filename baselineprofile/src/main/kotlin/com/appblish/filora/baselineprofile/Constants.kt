package com.appblish.filora.baselineprofile

/**
 * Application id of the "standard" Play-default flavor under test.
 * Keep in sync with `:app` `applicationId` (the "fullaccess" flavor adds a `.fullaccess` suffix).
 */
const val PACKAGE_NAME = "com.appblish.filora"
